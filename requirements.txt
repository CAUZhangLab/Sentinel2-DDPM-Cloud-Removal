# Runtime dependencies for the model and evaluation scripts.
# Install a platform-appropriate PyTorch build when CUDA acceleration is required.
numpy>=1.24,<3.0
torch>=2.0,<3.0
torchvision>=0.15,<1.0
tqdm>=4.65,<5.0
scikit-image>=0.21,<1.0
